# deps
node_modules/

# next
/.next/
/out/
next-env.d.ts

# env: never commit real keys
.env
.env.local
.env*.local

# runtime artifacts
/public/runs/
tsconfig.tsbuildinfo
*.log

# host: the link between this directory and a Vercel project, and its pulled env
.vercel

# scratch: one-off probes live here because a script outside the project cannot
# resolve the SDK it needs to probe
/tmp/

# os
.DS_Store
Thumbs.db
